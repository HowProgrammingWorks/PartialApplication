'use strict';

// Define function power - an alias to Math.pow().
const power = null;

// Implement function `square(n)`, which returns its argument squared.
// The function may or may not reuse function `power`.
const square = null;

// Implement function `powerOfThree(power)` using method bind().
// The function should return the number 3 to the power of the given argument.
const powerOfThree = null;

module.exports = { power, square, powerOfThree };
